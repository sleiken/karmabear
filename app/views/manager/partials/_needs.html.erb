<% if @needs.empty? %>
  no needs
<% else %>
  <% @needs.each do |need| %>
    <p><button type="button" class="btn man-dash-top-btn btn-block" data-toggle="modal" data-target=<%= "#viewNeedModal#{need.id}" %>><%= need.name %></button></p>
	  <%= render partial: 'manager/modals/view_need', locals: { need: need } %>
   <% end %>
<% end %>
