<h2 class="text-center">Needs:</h2>
<div class="col-xs-12">
  <% charity.needs.each do |need| %>
  	<%= link_to need.name, charity_need_path(charity, need), class: 'btn btn-info btn-block' %>
  <% end %>
</div>